import javax.swing.*;
import java.awt.*;

public class LoveFrame extends JFrame {
    LoveFrame()
    {
        JLabel bg = new JLabel(new ImageIcon("artifacts\\bg.jpg"));
        this.add(bg);
    }


}
